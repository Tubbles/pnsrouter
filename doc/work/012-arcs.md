# 012 Arcs

Status: implemented (2026-09-12); the LibrePCB host stays straight only by design

## Goal

Milestone 12: arc tracks as first class items and the two rounded corner modes, as KiCad's `SHAPE_ARC`, `PNS::ARC`, `ROUNDED_45` and `ROUNDED_90` do. A host with arc tracks (KiCad, Horizon) can then route among them, the placer can produce them, and the meanders get their round corner style back. The LibrePCB side stays straight only: a trace serialises no angle (`libs/librepcb/core/geometry/trace.cpp:236`) and the file format is stable, so LibrePCB keeps its corner mode mitered and refuses the rounded ones at the settings boundary until its format carries an arc trace.

Reference: `doc/reference/kicad/09-arcs.md`. The research behind lifting the hold is in `doc/log/2026-09-12.md`.

## Decisions

- Representation: KiCad's three point form, `start`, `mid`, `end` and a width, with no cached derived values. Every derived quantity (centre, radius, angles, length, bounding box) is computed on demand. Reason: the only form that is exact in `i32` nanometres and survives a round trip with KiCad's board file (note 09 sections 7.4 and 11.2).
- Hosts convert at the boundary. Two named constructors, `from_start_end_center` and `from_start_end_angle`, are documented as lossy and store nothing but the three points. A host that stores angles (LibrePCB) or centres (Horizon) must not rewrite an arc the commit diff did not list as updated.
- The `ArcRef` plan of note 01 section 14.3 is kept with the four changes of note 09 section 11.1: the reference carries the point's role, the chain order invariant is enforced, arcs are only reachable through a live iterator, and the accessors return `Option`.
- Every erratum a slice meets is either reproduced or fixed by an explicit, logged decision with a test naming it. Note 09 section 11.3 lists the intended fixes (E4, E8, E11, E22, E24, E26, E27, E29) and the intended reproductions (E38, the `MIN_PRECISION_IU` re-snap pending E18).

## Tasks

The eight slices of note 09 section 12, each leaving the crate green.

- [x] Reference note `doc/reference/kicad/09-arcs.md` (2026-09-12).
- [x] Slice 1, `ShapeArc` as a value type: `src/geometry/arc.rs` and the angle helpers in `src/geometry/math.rs` (2026-09-12). Exit met except `CollideNearlyFlatArcDoesNotOverflow`, which needs `Collide` and moves to slice 2, where it is already listed. Finding: `test_shape_arc.cpp:1395` to `:1495` holds seven `CalcArcCenter` cases, not the six note 09 section 8.1 counts, and all seven are mirrored. E1 logged in `doc/log/2026-09-12.md`.
- [x] Slice 2, arc collision primitives: `collide_point`, `collide_seg`, `nearest_point`, the four `nearest_points` overloads (2026-09-12). Exit met: `CollidePt`, `CollideSeg`, `CollideCircle`, `CollideNearlyFlatArcDoesNotOverflow` and `CollideArc`'s arc against arc quarter are mirrored and pass with no tolerance; E2 and E3 are both reproduced behind tests naming them. Finding: E3 does not ignore the arc's width as note 09 says, it applies the half width to the point and then loses the clamp to zero, so a wide arc reads as further from a rectangle the wider it gets. The two chain walking rows (`shape_collisions.cpp:636`, `:786`) and the rounded rectangle branch that delegates to the first of them move to slice 4.
- [x] Slice 3, arcs inside `LineChain`: the `shapes` and `arcs` fields, `ArcRef`, the predicates, the mutators and queries of section 11.3 (2026-09-12). Exit met: all eleven `test_shape_line_chain.cpp` arc cases mirrored and green, both property tests in, and E5, E6, E7, E10, E12, E13, E14 and E35 each behind a test naming it. Finding: the three argument `SHAPE_ARC` the `Split`, `Slice` and `NearestPointPt` cases build is the **centre, start, angle** constructor, not start, end, angle, and reading it the other way puts every point count in those cases off by one. Note 09 section 11.1's spelled out `is_arc_segment` is also wrong as written, needing an arc on both sides of the comparison, and its claim that the predicate needs no wrap case holds only for the role, not for finding the next vertex of a closed chain.
- [x] Slice 4, `Shape::Arc`, the six collision rows and `arc_hull` (2026-09-12). Exit met: `CollideArcToShapeLineChain` and all four quarters of `CollideArc` are mirrored, the hull table reproduces `pns_utils.cpp:86`'s truncation exactly, and E21 and E38 are each behind a test naming them. `CollideArcToPolygonApproximation` is the one case that could not be mirrored literally, needing `SHAPE_POLY_SET`, `TransformArcToPolygon` and a triangulation the crate has none of; it builds the same buffer outline directly instead. Finding: `ArcHull`'s whole circle branch (`pns_utils.cpp:79`) inflates the arc's **centre line** circle and so leaves the arc's own half width out of the hull, a defect note 09 does not list; reproduced, and slice 6 should look at it again. Also found: the polyline against polyline collision row is arc aware in two places (`shape_collisions.cpp:378`, `:427`), which slice 2 recorded as dead code and which slice 3 quietly made live.
- [x] Slice 5, `ItemBody::Arc` and the world: `add_arc`, `remove_arc_index`, `find_redundant_arc`, the arc loop in `add_line`, `assemble_line` and `follow_line`, `WorldGeometry::Arc` (2026-09-12). Exit met: `tests/arcs.rs` takes an arc track through the snapshot, the obstacle query, a line assembly, a removal and a recording round trip, `stickhub-extra-via.kicad_pcb`'s 180 arcs load with their layers, nets and widths, and E8, E22 and E36 each have a test naming them. Finding: `NODE::Add( LINE& )`'s arcs first order (E22) is the only producer that breaks `ClipVertexRange`'s shape order assumption, and fixing it forced `Line::shape_count` to stop answering `segment_count`, or an arc carrying line would fail the note 02 section 2.4 invariant by the arc's approximation point count. Also found: `shoveIteration`'s obstacle search never asks for `ARC_T` (`pns_shove.cpp:1650`), so slice 7 has to establish how its arc cases are reached before it rewrites them.
- [x] Slice 6, `CornerMode::Rounded45` and `Rounded90`: `build_initial_trace`'s two branches, `Direction45::from_arc`, the corner mode branches in the node, walkaround, optimizer and placer, the `has_arcs` gate (2026-09-12). Exit met except that the committed items are still the arc's chords, `fix_route`'s arc emission being slice 7. The table passes for all four modes and a rounded walkaround routes past a pad with an arc in the preview. Finding: erratum E28 is wrong. `mergeStep`'s arc guard is not dead, because the gate reads `aLine` while the guard reads `aCurrentPath`, the working copy `mergeStep` splices its own rounded bypasses into. Without it the chain invariants fail within a few hundred random rounded routes. Also found: `BuildInitialTrace` in a rounded mode returns **no arc at all** below a few micrometres, `Append( const SHAPE_ARC& )` demoting a two point polyline, and E18's re-snap mirrors the fillet about its chord rather than nudging its endpoint.
- [x] Slice 7, the placer's commit path and the shove (2026-09-12). Exit met with one substitution: a rounded shove pushes an existing **straight** track aside and commits an arc, an arc, straight, arc chain reaches the commit whole (E24), and the KiCad goldens do not move; what a rounded shove does **not** do is push an arc track aside, because it cannot see one. Finding: `shoveIteration`'s search asks for one kind per pass out of `{ SOLID_T, VIA_T, SEGMENT_T, HOLE_T }` (`pns_shove.cpp:1650`) and `ITEM::OfKind` is a bitwise test over disjoint bits (`pns_item.h:181`, `pns_node.cpp:243`), so both arc arms of the shove are dead code in KiCad; reproduced, and a route that overlaps an arc track in shove mode is committed in collision for the host's violation list to catch. Also: `NewGeometry::Arc` came a slice early and `CommitError` is gone with it; note 09 section 5.1 attributes `handlePullback`'s posture block to `reduceTail`, which has no arc branch at all.
- [x] Slice 8a, the crate half of slice 8: the dragger, the meanders and the pairs (2026-09-12). Exit met for the crate: an arc drags in all three modes and reaches a commit with its three points intact, a round style tune hits its target inside the tolerance the chamfered style already meets, and `test_meander_corner_radius.cpp:38`'s defaults are reproduced with `Round` as the default. Finding: an arc drag can only **shrink** the arc when one of its neighbours is not tangent to it, because the maximal inscribed circle `DragArc` clamps the cursor out of is then the circle the arc is already on; two tangent neighbours let it grow. Also: a walkaround or shove arc drag commits **two** arcs, `optimizeAndUpdateDraggedLine` splitting the chain at the preserved anchor and a vertex on an arc splitting the arc.
- [x] Slice 8b, the LibrePCB half (2026-09-12, on the fork's `pns-router` branch). Exit met with three of section 11.3's five FFI additions and a fourth that turned out to be the wrong shape. `PnsNewGeometryKind::Arc` and `PnsNewItem::mid` are in, and the applier refuses an arc with a `LogicError` naming the file format rather than dropping it or flattening it; `PnsShapeKind::Arc`, `PnsShape::mid` and `PnsArcGeometry` are **not**, because the snapshot side has no way to make one (a LibrePCB `Trace` has no angle and the curved copper that exists is flattened before it crosses), so they would have been three types nothing could write or test. The preview needed no flattening step either: `PnsPreviewItem` already crosses the `LineChain`'s own point list, which holds an arc's approximation at 1000 nm, five times tighter than the `maxArcTolerance()` the section proposed. The corner mode stays the boolean `corner_mode_90`, which is the settings boundary refusal itself. Finding: the arcs milestone reaches LibrePCB as exactly two non exhaustive matches, `WorldGeometry::Arc` in the snapshot stats readback and `NewGeometry::Arc` in the commit conversion, and the second one was silently taking the `else` arm into a straight trace across the arc's chord, so an unrefused arc would have been stored as its chord and an updated one would have deleted a trace and put nothing back. `doc/librepcb-integration.md` section 4.9 has the whole boundary and the list of what an arc trace would take when LibrePCB's file format next opens. Not done here and left for the branch that has it: `to_meander_request` and the `PnsMeanderSettings` doc live on `pns-router-pairs`, not on `pns-router`, and that branch already passes `MeanderStyle::Chamfer` by name, so only the doc comment's reason is stale.

The TODO entries this work item absorbed on 2026-09-12: the milestone 1 arc bullet (`SHAPE_ARC`, `ArcHull`, the arc reference vector, `SelfIntersectingWithArcs`, the rounded corner modes), `LineChain::nearest_point`'s `aAllowInternalShapePoints` flag, the `EDA_ANGLE` and `RotatePoint` bullets, and "arcs throughout the shove are marked `TODO(arcs)`".

## Acceptance

A trace routed in `Rounded45` between two pads around an obstacle commits an arc whose three points are exact integers, replays from its recorded session to the same commit, and an existing arc track is walked around and dragged in all three modes.

Met on 2026-09-12 by `tests/arcs.rs` (`a_rounded_walkaround_carries_an_arc_and_clears_the_obstacle`, `a_rounded_shove_commits_an_arc_and_pushes_a_track_aside`, `fix_route_commits_the_straight_between_two_arcs_erratum_e24`, `a_rounded_session_that_commits_an_arc_replays_to_the_same_commit`) and `tests/arc_drag.rs`. The criterion as first written also asked for an existing arc track to be **shoved aside**; that is not met and not meant to be, because KiCad's shove never searches for arc obstacles (note 09 erratum E40, reproduced in slice 7), so a line shoved across an arc track is committed in collision for the host's DRC to find. The one entry deviation that would change that is recorded in `SUGGESTIONS.md` for the user to decide.

## What the fixture cannot cover

No KiCad regression case exercises an arc. The corpus has one board with arc tracks, `stickhub-extra-via.kicad_pcb` with 180 of them, and no case replays it (note 09 section 8.3). KiCad's own unit tests cover `SHAPE_ARC` and the arc parts of `SHAPE_LINE_CHAIN` well (27 and 13 cases) and cover nothing above them: `DIRECTION_45::BuildInitialTrace` has no test in KiCad at all, and the four PNS unit tests never construct an arc. So slices 1 to 4 stand on mirrored KiCad tests and slices 5 to 8 stand on the port's own scenarios and recorded sessions, which is the footing milestone 11 had.
